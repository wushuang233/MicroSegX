ARG BASE_IMAGE_TAG
FROM microsegx/enforcer_base:${BASE_IMAGE_TAG}

# Dockerfile to create controller+enforcer container image

COPY stage /

LABEL microsegx.image="microsegx/controller+enforcer" \
      microsegx.role="controller+enforcer"

ENTRYPOINT ["/usr/local/bin/monitor", "-d"]
